#version 450
#extension GL_ARB_separate_shader_objects : enable

struct Atom {
  vec3 pos;
  float radius;
  vec3 colour;
};

layout(std430, binding=1) buffer Atoms {
  Atom atoms[];
} a;

layout (local_size_x = 64) in;

void main() {
  uint id = gl_GlobalInvocationID.x;
  //a.atoms[id].pos += vec3(cos(id * 0.1) * 0.1, sin(id * 0.1) * 0.1, 0.0);
}
